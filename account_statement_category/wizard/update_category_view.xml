<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_account_statement_line_mass_update_form" model="ir.ui.view">
        <field name="name">account.statement.line.mass.update.form</field>
        <field name="model">account.statement.line.mass.update</field>
        <field name="arch" type="xml">
            <form string="Mass Update Categories">
                <group>
                    <field name="category_id" domain="[('parent_id', '=', False)]" />
                    <field
                        name="subcategory_id"
                        domain="[('parent_id', '=', category_id)]"
                    />
                </group>
                <footer>
                    <button
                        name="action_apply"
                        string="Apply"
                        type="object"
                        class="btn-primary"
                    />
                    <button string="Cancel" class="btn-secondary" special="cancel" />
                </footer>
            </form>
        </field>
    </record>

    <record
        id="action_account_statement_line_mass_update"
        model="ir.actions.act_window"
    >
        <field name="name">Update Category</field>
        <field name="res_model">account.statement.line.mass.update</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field
            name="binding_model_id"
            ref="account.model_account_bank_statement_line"
        />
        <field name="binding_view_types">list</field>
    </record>
</odoo>
